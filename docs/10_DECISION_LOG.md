# 10 Decision Log

## Decisions 001-037

001. Burn control uses Policy Fixed Burn Target values by Economy Status.
002. Burn Target v1.0 fixed values: NORMAL 10.0%, WATCH 10.4%, WINTER 10.8%, EMERGENCY 11.2%.
003. Burn Target Count uses immutable `floor()`.
004. Race ranking ties use deterministic tie-breaker only.
005. Tie-break score is SHA-256 of race_seed, horse_uuid, and race_engine_version.
006. Race seed uses Server Commit-Reveal in v1.0.
007. Each race has exactly one independent race_seed and seed_hash.
008. Race Participant Snapshot is created at Daily Batch start and is immutable.
009. Marketplace locks during Daily Race Batch.
010. Purchase Session uses immediate fund locking.
011. Assignment Priority is P2P, then Day0 Mint fallback, then Refund.
012. P2P Horse Queue uses Oldest Listed Horse First.
013. Buyer Queue uses Oldest Purchase Session First.
014. Horse current_day increments only after Race Survival.
015. AI Profit Taking creates market listing only and does not transfer ownership.
016. AI Profit Taking Selection uses Liquidity Policy Table and deterministic sort.
017. Owner Listing Limit uses deterministic relaxation: max 1, then max 2 in v1.0.
018. Revenge Buff has one active buff per user with refresh, no stack.
019. Revenge Buff modifies final_score only as Modifier Layer.
020. Buff Policy v1.0: N 30% +4, R 50% +7, SR 20% +10.
021. MLM Reward is Burn Recovery Incentive: direct referrer only, 10 USDT, Level 1.
022. Day7 Buyback: 200 USDT over 7 payments, Payment 1 starts D+1, Memorial NFT after all paid.
023. Ledger account structure includes user, reserve, settlement, deposit, and withdrawal clearing accounts.
024. Day0 Mint Reserve Allocation v1.0: Buyback 93.60, MLM 5.40, Operating 0.70, Emergency 0.30.
025. Economy Status is determined by policy thresholds, AI only recommends.
026. Economy Status uses Stability Rule and Emergency Lock Period.
027. Daily Settlement Batch order v1.0 is fixed by batch_algorithm_version.
028. Admin Recovery Procedure is required for failed/partial failed batches.
029. Database spec scope is major columns, constraints, indexes, RLS, immutability; not full DDL.
030. API spec scope is architecture-level; OpenAPI is generated by Claude Code.
031. Repository structure and execution boundary are fixed.
032. Definition of Done and Test Policy are fixed.
033. Reserve Allocation was corrected to cover expected Buyback liability at 46.8% Day7 Arrival Rate.
034. Race Engine v1.0 final_score uses the fixed additive modifier formula and deterministic random_modifier.
035. Horse Generation v1.0 is deterministic with independent type/rarity draws, normal-distribution abilities, weighted base_ability_score, and immutable DNA.
036. Daily Training v1.0 is a one-race modifier with snapshot cutoff, no permanent ability increase, and deterministic LUCK random range adjustment.
037. Deposit and Withdrawal v1.0 supports USDT only, defaults to Polygon PoS USDT, uses Ledger for all balance changes, and excludes multi-chain support.

## Decisions 038-051 (2026-07-02, Owner)

038. Race formation v1.0: one race per day containing all ACTIVE horses. At large scale (e.g., 1,000,000 horses) the system may auto-split into multiple internal races for performance, but they are treated logically as one race with one global deterministic ranking.
039. Weather and Track Condition are derived deterministically from the committed `race_seed` via SHA-256 (Commit-Reveal). Weather is one of SUNNY, RAIN, CLOUDY, STORM. Track Condition uses the same SHA-256 derivation mechanism. AI, admin, and manual input are forbidden.
040. Condition and Fatigue use a daily deterministic recurrence: `condition_today = condition_yesterday + training_effect - fatigue`; `fatigue_today = fatigue_yesterday + training_cost - recovery`. Results clamp to the spec modifier ranges (condition_modifier -3.00..+3.00, fatigue_modifier -5.00..0.00). Exact numeric parameters (training_cost, recovery, initial values) are versioned in policy.
041. Valid direct referrer for MLM Reward = referrer with status ACTIVE only. BANNED, SUSPENDED, DELETED, self-referral, and referral loops are invalid; no MLM payment occurs.
042. Buyback payments are daily: due dates are D+1 through D+7 after Day7 Clear.
043. Purchase session lifecycle: cancellable until Marketplace Lock; locked sessions enter the batch; sessions unassigned in the batch are refunded and marked EXPIRED at batch completion. Purchase sessions do not persist across batches.
044. Economy metrics are deterministic formulas defined in economy_policy_version. v1.0: `p2p_match_rate = assigned P2P count / P2P listing count`; `rebuy_rate = burned owners repurchasing within 24h / burned owners`; `gmv_change_rate = (GMV_today - GMV_yesterday) / GMV_yesterday`. All remaining metrics (cash_coverage_ratio, buyback_liability_ratio, forecasted_cash_coverage) SHALL be formalized as deterministic formulas in the same style and submitted for owner approval before Phase 9 implementation.
045. Stress tests are deterministic parameterized scenarios. v1.0: Winter 30 = Mint demand -30%; Winter 90 = Mint demand -90%; Mass Withdrawal = 20% of all wallet balances withdrawn. Remaining scenarios (Base, High Survival, Low Burn, P2P Freeze, Buff Overpower) SHALL be parameterized in the same style and submitted for owner approval before Phase 9 implementation.
046. "AI" in this system means Deterministic Policy Engine (ordinary deterministic algorithms). LLMs (ChatGPT, Claude, Gemini, or any generative model) are forbidden in all production decision paths.
047. Daily batch time 20:00 is MYT (UTC+8). `batch_date` is defined in MYT.
048. Deposits use per-user HD wallet derived addresses. Shared deposit address is rejected.
049. Memorial NFT is minted on-chain as a permanent memorial. DB-only record is rejected. Chain and token standard default to Polygon PoS / ERC-721 pending final owner confirmation before Phase 7.
050. Horse names are generated by a deterministic Name Generator (not AI): Bloodline + Prefix + Suffix (e.g., Royal Thunder, Black Wind, Golden Storm), derived from mint_seed. Horse images are out of scope for v1.0 (future: Stable Diffusion / Flux generation).
051. A user may hold at most 10 concurrent purchase sessions (maximum lock 10 × 177.16 = 1,771.60 USDT).

## Decisions 052-055 (2026-07-03, Owner)

052. LUCK training (resolves P7): option (a). No fourth training type in v1.0. When a LUCK-type horse performs ANY of SPEED_TRAINING / POWER_TRAINING / RECOVERY_TRAINING, its LUCK trait activates for that race only: random_modifier range changes from -3.00/+3.00 to -2.00/+4.00. training_modifier follows the normal rules of the chosen training type.

053. Weather / Track derivation parameters (resolves P1):
- Weather probabilities: SUNNY 40%, CLOUDY 30%, RAIN 20%, STORM 10%.
- Track conditions: FAST, GOOD, SOFT, HEAVY. Probabilities: GOOD 40%, FAST 25%, SOFT 25%, HEAVY 10%.
- weather_modifier by (weather, horse_type):
  SUNNY: SPRINTER +2.00, POWER +0.50, BALANCED +1.00, ENDURANCE +0.00, LUCK +0.50
  CLOUDY: SPRINTER +0.50, POWER +0.50, BALANCED +1.00, ENDURANCE +0.50, LUCK +0.50
  RAIN: SPRINTER -1.50, POWER +2.00, BALANCED +0.00, ENDURANCE +1.00, LUCK +0.50
  STORM: SPRINTER -2.00, POWER +1.00, BALANCED -0.50, ENDURANCE +2.00, LUCK +1.00
- track_modifier by (track, horse_type):
  FAST: SPRINTER +2.00, POWER +0.00, BALANCED +0.50, ENDURANCE -1.00, LUCK +0.50
  GOOD: SPRINTER +0.50, POWER +0.50, BALANCED +1.00, ENDURANCE +0.50, LUCK +0.50
  SOFT: SPRINTER -1.00, POWER +2.00, BALANCED +0.00, ENDURANCE +1.00, LUCK +0.50
  HEAVY: SPRINTER -2.00, POWER +1.00, BALANCED -0.50, ENDURANCE +2.00, LUCK +1.00

054. Condition / Fatigue parameters (resolves P2):
- Initial values at mint: condition 80.00, fatigue 0.00. Both clamp to 0..100.
- training_cost (fatigue increase): SPEED +8, POWER +8, RECOVERY +3.
- daily_natural_recovery = 5; RECOVERY_TRAINING additional_recovery = 7 (total 12 on recovery days).
- training_effect (condition increase): SPEED +1, POWER +1, RECOVERY +3.
- race_fatigue_cost = +5 per race participation.
- Deterministic evaluation order per day: fatigue_today = clamp(fatigue_yesterday + training_cost + race_fatigue_cost - recovery, 0, 100); condition_today = clamp(condition_yesterday + training_effect - fatigue_today, 0, 100).
- condition_modifier mapping: >=90 -> +3; 80-89.99 -> +2; 70-79.99 -> +1; 50-69.99 -> 0; 30-49.99 -> -1; 10-29.99 -> -2; 0-9.99 -> -3.
- fatigue_modifier mapping: 0-10 -> 0; 10.01-25 -> -1; 25.01-40 -> -2; 40.01-60 -> -3; 60.01-80 -> -4; 80.01-100 -> -5.

055. Horse Name Generator (resolves P3): 40 prefixes and 40 suffixes fixed by owner (see packages/race-engine name generator). Display name = Prefix + Suffix; Bloodline is an internal attribute that weights vocabulary pools. Duplicate names are NOT allowed: collisions append Roman numerals (Royal Thunder II, III, ...), and beyond the Roman range a short generation code is appended (Royal Thunder G7).

## Decision 056 (2026-07-03, Owner)

056. horse_type_modifier is fixed at 0.00 in Race Engine v1.0 (resolves P8). Horse type strengths are already expressed through weather_modifier, track_modifier, training_modifier, and the LUCK-type random range adjustment; an independent horse_type_modifier would double-count type traits. The term remains in the final_score formula but its value is always 0.00 in v1.0. AI/Admin/User cannot change it. A future race_engine_version (v1.1+) may reintroduce a non-zero value.

## Decision 057 (2026-07-03, Owner)

057. Revenge Buff applies to exactly ONE race (resolves P9, option a). The buff auto-applies to the horse received in the user's next successful Assignment, is saved into that horse's next Race Participant Snapshot as the buff snapshot, adds +4/+7/+10 to that race's final_score, and becomes CONSUMED after that race regardless of Survive or Burn. Failed/refunded assignments do not consume it. Lifecycle: ACTIVE -> APPLIED (on assignment) -> snapshotted for the next race -> CONSUMED. Rationale: matches the Expected Buff Modifier +6.7 design; multi-race protection would inflate Day7 arrival rate and Buyback liability; keeps replay/audit simple.
Implementation note: CONSUMED is executed at snapshot inclusion (not after race completion). Snapshot inclusion is the irreversible commitment point — the immutable snapshot guarantees the buff affects exactly that one race whatever the outcome, so the two timings are financially and game-mechanically equivalent, and snapshot-time consumption is atomic with the snapshot itself.

## Decisions 058-059 (2026-07-03, Owner)

058. Economy metrics v1.0 (resolves P4). All metrics are calculated deterministically from ledger, buyback, marketplace, and batch records. LLM-based forecasting SHALL NOT be used in v1.0.
- Platform Liquid Reserves = PLATFORM_BUYBACK_RESERVE + PLATFORM_MLM_RESERVE + PLATFORM_EMERGENCY_RESERVE.
- cash_coverage_ratio = Platform Liquid Reserves / Next 30 Days Scheduled Buyback Payments (30-day window).
- buyback_cash_coverage_ratio = PLATFORM_BUYBACK_RESERVE / Next 30 Days Scheduled Buyback Payments (tracked separately for normal operations).
- buyback_liability_ratio = Total Unpaid Buyback Liability (all buyback_schedule_payments with status != PAID) / Platform Total Reserves (all four reserves). Lower is safer; >= 1.0 requires attention.
- forecasted_cash_coverage = Projected Liquid Reserves After 30 Days / Projected 30 Days Buyback Payments, where projected mint inflow uses avg_day0_mint_count_last_7d x 30 x reserve allocation amounts (deterministic projection input), minus projected MLM and operating usage; projected payments = scheduled next 30d + projected new buyback liability payments.

059. Stress test scenarios v1.0 (resolves P5). Daily runs, 30-day simulation horizon. Common pass conditions: cash_coverage_ratio >= 1.00 every day, buyback_cash_coverage_ratio >= 1.00 every day, no negative reserve balance, all due buyback payments payable.
- Base: current trend continues (7d averages for mints/match/rebuy, 30d averages for day7 arrival and burn). Pass: cash_coverage_ratio >= 1.20 for 30 days.
- High Survival: Day7 Arrival Rate +10 percentage points. Pass: buyback_cash_coverage_ratio >= 1.00 for 30 days.
- Low Burn: burn count -30% (arrival rises accordingly). Pass: cash_coverage_ratio >= 1.00.
- P2P Freeze: P2P demand = 0 for 7 days (match rate 0, only Day0 fallback if allowed, listings unassigned). Pass: all unassigned sessions refundable, no negative locked balance, buybacks payable, cash_coverage_ratio >= 1.00.
- Buff Overpower: buff holders' effective survival impact +15 percentage points on projected arrivals (input: buff consumption avg last 7d). Pass: buyback_cash_coverage_ratio >= 1.00 for 30 days.
- Mass Withdrawal: 20% of all wallet balances withdrawn (Decision 045).
- Winter 30 / Winter 90: mint demand -30% / -90% (Decision 045).
Economy Status linkage on failure: Base fail -> at least WATCH; High Survival fail -> at least WINTER; P2P Freeze fail -> at least WATCH; Buff Overpower fail -> at least WINTER; any scenario with a buyback payment shortfall -> EMERGENCY. Stress results influence Tomorrow Economy Status per economy_policy_version.

## Decision 060 (2026-07-03, Owner)

060. Large withdrawal Admin Review (resolves E14). Withdrawals of 1,000 USDT or more require Admin Review before broadcast. Approval requires TWO distinct admins — one FINANCE_ADMIN and one SUPER_ADMIN — the same dual-approval standard as the Recovery Procedure, because this is a financial system.
Implementation note: approvals are recorded append-only in withdrawal_review_approvals (unique per admin and per role for each withdrawal); the row re-enters the broadcast queue only after both roles have approved, and an approved withdrawal is never re-routed to review. A single admin may reject; rejection refunds the full locked amount (the conservative action).

## Decision 061 (2026-07-03, Owner)

061. Withdrawal network fee = actual gas cost pass-through. No fixed fee is adopted (Polygon, BSC, and future chains all differ). The user bears the actual network cost; the platform takes NO profit from it and the fee SHALL NOT be booked as Ledger revenue — this is gas settlement, not income.
Implementation note: net_amount must be fixed before signing, so the charged fee is the pre-broadcast estimate transfer_gas_limit x maxFeePerGas converted to USDT via an ops-configured native/USDT rate, rounded UP at the token's 6 decimals (the platform never subsidizes gas). Deducted from requested_amount into net_amount; the locked funds remain in PLATFORM_WITHDRAWAL_CLEARING as the external-world boundary (no revenue entry).

## Decision 062 (2026-07-03, Owner)

062. Launch chain confirmed: Polygon PoS USDT with confirmation_count = 128 blocks (the whole v1.0 design is Polygon-based). RPC provider selection is an implementation/ops matter, not a spec item; the owner recommends QuickNode (commercial-grade). The BSC-switchable config structure remains as required by 07_API.md.

## Decision 063 (2026-07-03, Owner)

063. Memorial NFT final specification (resolves P6). Chain/standard: Polygon PoS, ERC-721. Mint timing: only after ALL seven buyback payments have completed. Metadata minimum fields: Horse Name, Horse UUID, Bloodline, Horse Type, Rarity, Generation, Day7 Date, Buyback Completion Date, Race Count, Owner, Version. Transfer: YES — freely transferable, but a transferred NFT never returns to the game; it is a pure commemorative asset with no in-game effect.
Implementation note: Bloodline maps to the name generator's bloodline component (the horse name prefix, per Decision 055); Generation = horse_generation_version; Owner = owner user UUID at memorialization; Version = memorial metadata schema version (memorial_v1.0); Race Count = the horse's race_participant_snapshots count; Buyback Completion Date = paid_at of payment 7. Token id is derived deterministically from the memorial UUID so re-minting after a crash is structurally impossible (the contract rejects an existing token id).

## Decision 064 (2026-07-03, Owner)

064. Withdrawal amount decimals are restricted to 6 decimal places at the API (POST /wallet/withdraw). Rationale: Polygon USDT has 6 on-chain decimals; accepting 8 creates unrepresentable amounts. The broadcaster keeps its refund-on-unrepresentable guard as defense in depth.

## Decision 065 (2026-07-03, Owner)

065. Notification specification v1.0 (resolves E17). Channel: In-App notifications ONLY (Email / Push / Telegram / LINE are future scope). Thirteen notification types are adopted: DEPOSIT_CONFIRMED, ASSIGNMENT_COMPLETED, TRAINING_COMPLETED, RACE_RESULT_READY, HORSE_BURNED, REVENGE_BUFF_GENERATED, BUYBACK_PAYMENT_PAID, BUYBACK_COMPLETED, MEMORIAL_NFT_MINTED, WITHDRAWAL_COMPLETED, WITHDRAWAL_FAILED, MARKETPLACE_LOCKED, MARKETPLACE_REOPENED. The Japanese title/body templates fixed by the owner are recorded verbatim in packages/domain (NOTIFICATION_TEMPLATES_V1) with {placeholder} interpolation.
Implementation note: notifications are emitted at each event site with a deterministic dedupe_key (unique index) so batch retries and crash re-runs can never duplicate a notification. MARKETPLACE_LOCKED / MARKETPLACE_REOPENED are single broadcast rows (user_id null, readable by all authenticated users) rather than per-user fan-out.

## Decision 066 (2026-07-03, Owner)

066. Training API added to 07_API.md: POST /api/v1/horses/{id}/training with body { training_type: SPEED_TRAINING | POWER_TRAINING | RECOVERY_TRAINING }. Rules: one training per horse per effective_race_date (DB-enforced); only the horse's owner may train; training never permanently modifies ability. Timing (v1.0, owner): while the marketplace is LOCKED (Batch Lock onward) the day's intake is CLOSED — the request is rejected with MARKETPLACE_LOCKED to avoid confusion. While OPEN, the training applies to the next race to run: today's race if today's batch has not completed yet, otherwise tomorrow's. Errors: HORSE_NOT_FOUND, NOT_HORSE_OWNER, TRAINING_ALREADY_EXISTS, RACE_SNAPSHOT_ALREADY_CREATED, INVALID_TRAINING_TYPE, MARKETPLACE_LOCKED.

## Decision 067 (2026-07-03, Owner)

067. Admin Recovery API surface added to 07_API.md: GET /api/v1/admin/recovery (list), GET /api/v1/admin/recovery/{id} (detail including recovery_logs), POST /api/v1/admin/recovery/{id}/approve (existing), POST /api/v1/admin/recovery/{id}/execute. Auth: Admin JWT + role (FINANCE_ADMIN / SUPER_ADMIN). Rules unchanged from the constitution: dual approval by two distinct admins; recovery can never mutate posted ledger entries, rerun a race with changed inputs, or replace a seed/snapshot; every action writes recovery_logs. Errors: RECOVERY_NOT_FOUND, RECOVERY_ALREADY_APPROVED, RECOVERY_REQUIRES_DUAL_APPROVAL, INVALID_RECOVERY_STATE, FORBIDDEN.

## Decision 068 (2026-07-03, Owner)

068. Frontend hosting changed from Vercel to Render (render.com). The execution boundary is otherwise UNCHANGED: Render serves the user/admin UI and the lightweight API mount (the same @sevendays/api-contracts registry); all financial/batch/settlement/recovery logic remains exclusively on Google Cloud Run; the Service Role Key and wallet keys never reach the client bundle (the existing build-time bundle inspection still enforces this). Render runs the app as a persistent Node server (suits the Postgres session-pooled connections better than serverless). Deployment is Blueprint-based (render.yaml, Singapore region — closest to Supabase ap-south-1).

## Decision 069 (2026-07-04, Developer under owner mandate — economy revision v1.1)

069. Economy revision v1.1. Context: the 100,000-user simulation and a demand-stop drill measured the actual Day7 arrival rate at ~49.4% (floor rounding + revenge buffs push it above the 47.8% geometric model), which made the buyback pool structurally under-reserved by ~5.3 USDT per mint — the aggregate RTP was ~100.6%, payable only while mint volume kept growing. In the drill, a cold demand stop defaulted the buyback pool 9 days later with 279 missed payments. The player-facing promise (buyback 200 over 7 payments, price curve, purchase lock 177.16) is UNCHANGED. Full data: ECONOMY_REVISION.md.
Changes:
- Burn Target ladder +0.7pt: NORMAL 0.107 / WATCH 0.111 / WINTER 0.115 / EMERGENCY 0.119 (daily survival 90% -> 89.3%).
- Day0 mint fee 2 USDT (total charge 102): 1 to PLATFORM_OPERATING_RESERVE, 1 to the buyback buffer. Fits inside the existing 177.16 lock.
- P2P fee 2% SELLER-side (buyer pays the listed price): 1% operating, 1% buyback buffer. This amends the constitution line "P2P platform trading fee is always 0"; the fee is a fixed policy value — the forbidden "P2P fee setting API" remains forbidden.
- Mint coverage gate: Day0 minting is allowed only while the buyback reserve covers the stop-scenario liability (deterministic factor table, 3.5% safety margin over the geometric model, matching the measured arrival excess). Engine-enforced at Step 25/26.
Authorization note: approved by the developer under the owner's standing mandate to correct any >100% RTP condition; prepared for formal owner ratification with ECONOMY_REVISION.md.

## Decision 070 (2026-07-04, Owner)

070. Worker hosting moved from Google Cloud Run to Render (following Decision 068 for the frontend). All eleven worker roles consolidate into ONE always-on Render PRIVATE service (services/render-worker) with the scheduler IN-PROCESS: the daily batch trigger is self-healing ("today's batch due + no batch_runs row" evaluated every 30s tick; a FAILED batch is never auto-retried — Admin Recovery only), chain loops run on intervals, and every job is idempotent so restarts/double-fires are harmless. Cloud Scheduler and Pub/Sub are not used in v1.0. The execution boundary is unchanged in substance: financial/batch logic runs ONLY in this private worker (no public URL); the web service serves UI + the lightweight API mount. The Cloud Run deployment path (infra/cloudrun, the ten per-role services, Dockerfile) remains in the repository as the documented scale-out option. Rationale: single-platform operations (Render + Supabase), and the GCP billing path was blocked for the owner; capacity analysis showed the daily-batch workload fits a single worker well beyond the near-term user scale, with the true scale limits (DB, set-based SQL debt) unaffected by this choice.

## Decision 071 (2026-07-04, Owner)

071. Login methods: MetaMask (Sign-In with Ethereum via the Supabase "Web3 Wallet" provider) and Google OAuth become the primary sign-in options. Email+password remains available as a secondary fallback (the owner's admin account is email-based; full removal can follow after identity migration). No backend changes were required: all providers issue standard Supabase JWTs, and the API bridge already verifies ES256 via JWKS and provisions users without an email claim.

## Decision 072 (2026-07-04, Owner)

072. Cross-linking of login methods. A MetaMask-first user can link Google on the account page (native Supabase identity linking — same auth user, requires the dashboard "Manual Linking" toggle); a Google/email-first user can link MetaMask via a personal_sign proof (fresh timestamped message bound to the logged-in user, verified server-side) recorded in user_wallets. A Web3 session whose wallet is linked resolves to the linked game account at the API bridge (aliasing) — no second account is ever created for a linked wallet, and a Web3-first account claims its wallet on first login so it can never be linked elsewhere (one wallet = one account, DB-unique). Endpoints: GET /api/v1/account/wallets, POST /api/v1/account/link-wallet, POST /api/v1/account/unlink-wallet.

## Decision 073 (2026-07-06, Owner)

073. The Daily Derby live experience. The 20:00 MYT daily settlement is presented to users as "THE DAILY DERBY", a ~100-second live show (owner-approved ADR-006/007/008 drafts in `GPTとの会話/`, adapted as follows). Sequence: full-screen seven-segment digital countdown from T-3min (self-drawn SVG, no external font; turns red for the final 30 seconds), at exactly 20:00 a real horse-racing fanfare (owner-supplied audio, 16.8s) covers the opening steps, looped hoofbeat audio during the race-run segment (T+17-30s), then a high-speed terminal log flood: RACE TURN (burns in red, survivors in green, value progression from PRICE_TABLE_V1, DAY7 clears in gold), P2P MARKETPLACE TURN (sell orders, buy orders, matches with a global MATCHED counter, Day0 mints handed to buyers), REWARDS TURN (MLM 10 USDT payouts per Decision 021, Revenge Buff drops), ending with "TODAY RACE END" and a personal-result finale card that renders the user's actual NFT horse art (deriveNftLook). v1.0 scope decisions: no country flags or geo data (anonymized ticker only: horse names and masked user ids, never wallet addresses); no emoji anywhere in the show (fixed-width terminal tags + per-tone color ticks instead); polling-based sync, no WebSocket; sound on by default with a visible toggle. Failure mode shows only the calm safe-mode message (no panic wording). Until the batch-status API is wired (pre-launch task), the prototype runs on deterministic fixture data and is intentionally mounted on the /races page with its simulation controls so the owner can review it repeatedly; the visual-QA route /dev/derby-preview (404 in production) drives all states via a simulated clock. Related ADRs not yet adopted: MLM restructure (owner will specify later; Decision 021/041 remain in force), Manual Marketplace (under legal-risk discussion), Item System ADR-009 (queued after this feature).

## Decision 074 (2026-07-06, Owner)

074. Support Allowance (Burn Recovery Support) v2 — placement-based 7-TIER unilevel. Replaces the payout rule of Decision 021 (direct referrer, 10 USDT, one level); Decision 041's validity semantics carry over; the per-mint reserve allocation (5.40 to PLATFORM_MLM_RESERVE, Decision 024) is unchanged. Structure: TWO relations. (1) The immutable sponsor relation (referral link captured at signup; existing users.direct_referrer_user_id) drives unplaced-pool ownership and tier unlocking. (2) A placement tree is the payout path: referred users land in their sponsor's UNPLACED POOL and are hand-placed EXACTLY ONCE — directly under the sponsor (unlimited width) or under any node of the sponsor's own placement subtree; placement is immutable for everyone except an audited admin exception (UI must carry a strong "cannot be changed" warning); unplaced users can still refer and pre-arrange their own subtree, which attaches wholesale when they are placed; no placement deadline. Payout: on each burn, walk UP the placement tree at most 7 tiers and pay from PLATFORM_MLM_RESERVE — Tier1 3 USDT, Tier2 2 USDT, Tiers3-7 1 USDT each (total cap 10 USDT per burn, unchanged). An ancestor at distance d is paid only if (a) ACTIVE (an invalid tier is unclaimed — no compression/roll-up) and (b) the ancestor's unlocked tier count >= d. Tier unlocking is VOLUME-based, not headcount-based (sybil resistance: Google-auth multi-accounts are free, horses are not): the metric is the combined current value (PRICE_TABLE_V1) of ACTIVE horses held by the ancestor's DIRECT referrals (sponsor relation), evaluated deterministically at every 20:00 batch — tiers downgrade automatically the moment volume is not maintained. Thresholds (USDT): Tier1 from 0 (always unlocked), Tier2 >= 3,001, Tier3 >= 5,001, Tier4 >= 10,001, Tier5 >= 30,001, Tier6 >= 50,001, Tier7 >= 70,001 (fractional volumes between integer bounds fall to the lower tier). ALL unpaid amounts (locked/invalid tiers, chains shorter than 7, unplaced users, tree roots) remain in PLATFORM_MLM_RESERVE — the ADR's 40/40/20 redistribution is REJECTED (it would drain the reserve structurally under EMERGENCY burn rates; retained amounts build the operator's safety surplus). User-facing naming: "サポート手当 (Burn Recovery Support)" — never "commission" / "MLM reward" / "referral reward"; the stated consideration is supporting the burned player's recovery, funded by the pre-funded per-mint reserve (legal framing and lawyer-review memo: LEGAL_REVIEW_MEMO.md; marketing rules: PRELAUNCH_COPY_RISKS.md R3). Implementation spec: MLM_REVISION.md (phases M-1..M-5).

Amendment (2026-07-06, Owner): the user-facing name is サポートボーナス (Support Bonus), superseding "サポート手当". The product worldview is "your stable supported a fellow player's recovery, so a support bonus is paid" — the MLM concept is deliberately diluted everywhere in the product: never say MLM / commission / referral reward in any user-facing surface; the network page is presented as a support/invite network, not an organization chart.

## Decision 075 (2026-07-07, Owner)

075. Champion Program terminology (adopts the naming part of the ADR-010 draft in GPTとの会話/). "Buyback" was the one word in the product that read as an asset-redemption right and therefore as an investment product; the substance has always been a fixed, outcome-conditional achievement award (reach Day7 through seven daily races -> 200 USDT in seven installments + Memorial NFT), so the presentation is renamed to match the substance. User-facing terms: Champion Program / Champion Reward / Champion Horse; Japanese チャンピオン報酬 (statuses show チャンピオン). Page route /buybacks moves to /champion (old URLs redirect); the two BUYBACK_* notification templates are reworded (amends the Decision 065 verbatim texts); landing stats, horse/stable statuses, wallet labels, admin note and the Daily Derby show lines follow. NOTHING internal changes: DB tables (buyback_schedules...), ledger transaction types, API paths (/api/v1/buybacks) and notification type KEYS stay as-is — the rename is presentation-only and the legal memo continues to describe the substance honestly (LEGAL_REVIEW_MEMO adds a 景品表示法 review topic: Champion Reward as 景品類/懸賞 analysis, since the reward framing shifts the relevant lens from redemption to prize regulation). ADR-010's re-proposal of the 40/40/20 unclaimed-support redistribution is REJECTED again — Decision 074's full retention in PLATFORM_MLM_RESERVE stands (the monthly surplus sweep to a future Community Growth Pool remains a recorded option, revisited no earlier than the Item System work). MLM naming stays サポートボーナス (nav: TEAM).

## Decision 076 (2026-07-07, Owner)

076. Visible marketplace + Manual listings (adopts the Smart/Manual Marketplace ADR with owner modifications). (a) The buy side stays reservation-based — purchase sessions (Decisions 010/043/051) are unchanged — but becomes VISIBLE on a new /market page: the user's own reservations, the global COUNT of tonight's pending buy reservations, the shelf of listed horses (art, name, day, ladder price), and a recent-matches feed (anonymized: horse name, price, masked ids — Daily Derby ticker display rules). (b) Manual listing: an owner may list an ACTIVE Day1-6 horse at the CURRENT ladder price only (free pricing rejected for v1.0 — no arbitrage against auto-assignment, no price manipulation surface); the listing is visible immediately and joins THE SAME deterministic matching queue as smart listings (Decision 012 ordering unchanged, oldest listed_at first; no artificial prioritization in either direction). (c) Market Lock (from the ADR): while manually listed the horse does not race — excluded from participant snapshots; current_day and value freeze; this structurally prevents "listed AND racing toward the Day7 Champion Reward". (d) Unlisting takes effect after the next batch (the listing stays matchable tonight; a sale wins over a pending cancel); listing operations are limited to one per horse per day; "parking" is possible but value-frozen and reduces Day7 arrivals (reserve-safe). (e) Settlement is identical to smart sales: matched in the 20:00 batch, 2% seller fee (Decision 069). (f) Smart Marketplace stays the default and unchanged (system-listed horses keep racing). Purpose on record: this is the primary mitigation for PRELAUNCH_COPY_RISKS R1 (user discretion + market visibility) ahead of the lawyer review. Implementation spec: MARKETPLACE_REVISION.md (P-1..P-4).

## Decision 077 (2026-07-07, Owner)

077. Tier unlocking switches from DIRECT-referral volume to ORGANIZATION volume with a personal requirement at the top (amends the unlock rule of Decision 074; everything else — placement payouts, retention, evaluation timing — stands). Metric 1, org volume: the combined current value (PRICE_TABLE_V1) of ACTIVE horses held by ACTIVE members in the user's PLACEMENT subtree down to 7 levels (= exactly the depth the user can earn from; deeper members count toward their own uplines' seven). Metric 2, direct volume: the Decision 074 metric (direct referrals only), unchanged. Unlock rule: T1 unconditional; T2 org>=10,000; T3 org>=20,000; T4 org>=50,000; T5 org>=250,000 AND direct>=30,001; T6 org>=400,000 AND direct>=50,001; T7 org>=600,000 AND direct>=70,001 (USDT, inclusive bounds; fractional volumes fall down). Rationale (Monte Carlo study, SUPPORT_TIER_SIMULATION.md, simulator scripts/tier-sim.mjs): the org-equivalent of the owner's field-tested direct thresholds grows super-linearly with tier (x2.3 at T2 to x8+ at T5); the top tiers do NOT separate on org volume alone (big organizations converge in shape), so T5-T7 additionally require the original field-tested direct thresholds — the leader must remain an active recruiter personally. Economics verified safe in all variants (expected payout 5.4-6.8 of the 10 cap; per-mint liability ~3 USDT vs the 5.40 reserve; no cap changes needed; unclaimed amounts still retained). Evaluation stays point-in-time at every 20:00 batch with automatic downgrade. Thresholds remain policy constants to be recalibrated on real post-launch data with the committed simulator.

## Decision 078 (2026-07-07, Owner)

078. Item System v1 framework (from ADR-009 + owner concept session). Core axis: deterministic horse-parameter x item compatibility — every item is a public, verifiable rule, never a hidden coefficient. (1) Catalog: ~30 sellable items in three price bands (basic 1-2 / standard 3-4 / premium 5-7 USDT, hard cap 8, integer prices only; average ~2.5-3 — sized so the standard band matches the expected value of ~+1 score point, keeping "worth buying but never erasing survival profit") plus 5 non-sellable burn-drop items (dropped alongside the Revenge Buff, non-tradable, price 0). (2) The owner's pachislot-style "setting 1-6" concept is adopted as the ITEM SETTING (item weather): a daily effectiveness coefficient x0.5-x1.5 (distribution 10/15/25/25/15/10 centered on 3-4) derived deterministically from the committed race seed and revealed with results — operator-steerable secret settings were explicitly REJECTED (they would break the deterministic/provably-fair constitution and create severe consumer-protection exposure); long-term balance changes go through public item_policy versions, announced prospectively, never reactive to who is winning. (3) Score formula v1.1: new item_modifier component (0..+6) so effects never clamp away against the training cap; random_modifier range extends to -3..+5 (Lucky-type shifts stack additively with the LUCK-training range). Stat items move condition/fatigue within their existing 0-100 clamps. Race engine version bumps; snapshots freeze applied items; old races replay unchanged. (4) Money: purchases park in a new PLATFORM_ITEM_CLEARING account; when the horse that used the item BURNS, the full item price funds the Support Bonus reserve (community funding from losses — owner's rule); when it survives, the full price goes to operating (the ADR's 60/20/20 split is REJECTED — reserves should keep headroom). Unused inventory stays in clearing as deferred revenue. (5) One item per horse per race, applied before snapshot, inventory model with Buy & Apply, usable alongside the Revenge Buff, no permanent stat gains, no effect on burn targets or seeds. Spec: ITEM_REVISION.md (catalog contents are owner-reviewable data, not part of this decision).

## Decision 079 (2026-07-07, Owner)

079. Item gifting + in-site transfer foundation. ALL items (including the five burn-drop items — the owner keeps them premium-strength as condolence for e.g. a Day6 177-USDT total loss one night short of Champion) can be GIFTED between users, addressed by the recipient's EMAIL (case-insensitive exact match, ACTIVE users only, synthetic MetaMask emails included). Implemented on a generic user_transfers ledger (asset_type ITEM now, USDT later) because the owner plans in-site USDT transfers between users (book-entry only — no on-chain movement); the same recipient resolution, idempotency, rate limiting (20 transfers/day v1) and notifications must serve both. Transfers are instant and irrevocable. Recorded caveats the owner accepted: email addressing is enumerable (mitigated by rate limits), and giftable burn-drops weaken the "the strongest items cannot be bought" claim (a catalog-level giftable flag allows tightening per item later).

## Decision 080 (2026-07-08, Owner)

080. Champion page consolidation + Champion League preview (adopts ADR-011 with one change: the ADR's /champions route redirects into the EXISTING /champion, so the champion experience lives on ONE page — the owner judged the old separate reward-schedule page "scattered" and wanted everything together). The page now carries: (1) a hero LOOP ANIMATION built on the owner's D-Logic "prediction theater" tech (engine.js analytic track geometry + race-canvas 2.5D/3D renderer with GLB horse and 2D auto-fallback), ported into public/champions/keiba/ with three additions — a VOID palette (black background, gold rail/effects per the ADR), metallic coats derived per horse from the NFT look hue (deriveNftLook bodyDeg -> chrome recipe fed to both the 3D texture-composite path and the 2D sprite path), and input coat/silks passthrough in the engine; seeds rotate on each finish for an endless loop, assets lazy-load via IntersectionObserver (11MB GLB); (2) the champion reward schedule (the former buyback view — its lingering "買い戻されます" copy was reworded to チャンピオン報酬, finishing Decision 075); (3) Hall of Champions fed by GET /api/v1/champions/hall (real DAY7_CLEARED/MEMORIALIZED horses with masked owners; a labeled sample set shows until the first real champion); (4) Champion League COMING SOON blocks: unlock at 10,000 active users, weekly races max 18 horses, classes Maiden->1/2/3勝->G3/G2/G1, promotion + retirement (G1 win or 10 league races), prize pool = 1% of weekly item-shop sales as a separate economy, fan pass 3 USDT — all presentation only, no gameplay implemented, and no betting/odds/gambling vocabulary (ADR legal rule). Nav gains CHAMPION.

## Decision 081 (2026-07-09, Owner)

081. AI customer service by email (port of the owner's proven betimail system at dev/Cusor/betimail). All support runs through support@sevendaysderby.com on Resend: inbound webhook (Svix-verified, deduped by message_id/webhook_email_id, body hydrated via the Resend inbound API) -> DeepSeek drafts a reply with tool-calling structured output {reply, confidence, needs_human, reason} -> approval queue (cs_messages) surfaced at /admin/support where the admin edits/approves/rejects; the reply threads via In-Reply-To. OPERATING MODE IS APPROVAL-FIRST: no auto-send code path exists yet (auto-send with a confidence threshold is a future decision after the owner has reviewed real AI drafts). Knowledge base (packages/api-contracts/src/cs/knowledge.ts) contains ONLY user-visible public information and enforces the honest-copy vocabulary (no 賭け/オッズ/配当/MLM/コミッション); the AI receives at most {registered, active horse count, horse names} about the sender — never balances, transactions or addresses, because email From is spoofable; concrete numbers are always redirected to in-site pages. Transactional mail: welcome email fires on first-login provisioning (fire-and-forget), Supabase Auth mail (password reset etc.) is switched to Resend SMTP by dashboard config (CS_SETUP.md). CS_TEST_MODE allowlist guards test sends, exactly like betimail.

## Decision 082 (2026-07-09, Owner)

082. Race conditions replace the item setting (owner: "これは競馬ゲームなのだから競馬を連想させるべき" — the pachislot-flavoured 設定1〜6 is retired). The nightly variation of ITEM effectiveness now comes from RACE CONDITIONS in racing vocabulary: weather (晴れ/曇り/雨/嵐) x track condition (高速/良/稍重/不良) x the NEW surface (芝60%/ダート40%). Weather and track were ALREADY core engine values (seed-derived, score-affecting, per Decisions 039/053) — the owner approved keeping their 4x4 form (richer than his initial 3x3 sketch, zero engine-economy risk); surface is a new seed derivation that affects items only. Each item carries a PUBLIC affinity (オールラウンド/芝巧者/ダート巧者/晴れ舞台/雨の鬼/高速馬場好き/道悪の鬼/荒天の大駆け) whose coefficient table stays inside v1's x0.5..x1.5 bounds with EV ~= 1.0 over the published distributions (economy-neutral, verified by test). Rare combinations get festival names (raceNightNameV2: 嵐の荒天決戦, 豪雨のダート決戦, 絶好の芝日和 …) for the reveal ceremony. Catalog v2 = total refresh at 35 (12/12/6/5): 26 proven items carried over, 9 retired (iron_horseshoe, morning_dew, carrot_bundle, endurance_wrap, sprint_spurs, veteran_blanket, twin_horseshoes, war_banner, golden_charm — kept as legacy effect branches + active=false rows so held units still replay), 9 condition-themed added (turf_spikes, dirt_shoes, rain_hood, firm_plates, mud_guards, turf_master_saddle, dirt_master_saddle, storm_emperor_cloak, mudlord_crown). storm_cloak/sunny_visor lose their in-effect weather branch (the response now lives ONLY in the affinity — no double dipping). item_policy_v2.0; races carries revealed weather/track_condition/surface (item_setting dropped pre-launch); /api/v1/items/settings becomes /items/conditions with JA labels + night names.

## Decision 083 (2026-07-11, Owner)

083. Login collapses to Google only; the /login page is removed. Rationale: the site is custodial, not a dapp — users never sign transactions (deposits go to generated custody addresses, prizes/buybacks are ledger entries, and even memorial NFTs mint to the custody wallet with user withdrawal as a future ops flow), so MetaMask login was pure SIWE authentication with no product value, and wallet-only accounts were second-class citizens (synthetic `{uuid}@user.sevendays` email → no welcome/CS mail, cannot receive item gifts since gifting targets emails — email is the identity backbone across MLM map, marketplace matching and CS). Email+password is retired with it. Every landing CTA now starts Google OAuth directly (official dark Google button; the hero button keeps its protected design and only gains the behaviour); unauthenticated visitors bounce to `/` instead of `/login`. UI-only and reversible: the Supabase Web3/email providers and `user_wallets` stay enabled, and the /account wallet-linking flow (Decision 072) survives as the future NFT-withdrawal address mechanism. Existing MetaMask/email registrations keep working via Google login on the same email (Supabase auto-merges identities, verified under Decision 071) or via support.

## Decision 084 (2026-07-11, Owner)

084. Web Push delivery goes live with two broadcasts: the nightly race reminder and the CS newsletter. Groundwork was Decision 083's PWA (manifest + no-cache service worker with a push handler); this decision adds the server side. Storage: `push_subscriptions` (browser PushSubscription keyed by unique endpoint, owner reassigned on re-subscribe for device handovers; unsubscribe and dead endpoints set `disabled_at`, rows are never deleted) and `push_broadcasts` (unique `broadcast_key` = idempotency claim, delete-forbidden). APIs: `/api/v1/push/public-key`, `/subscribe`, `/unsubscribe` (user auth). Client: the dashboard PWA tile silently syncs the browser subscription whenever permission is granted. (1) RACE REMINDER — owner-specified timing is FIVE MINUTES BEFORE the 20:00 MYT start (the race is short; a notification at start is too late), timezone-independent copy 「まもなく発走 — あと5分で本日のダービーが始まります」→ /races. The render-worker in-process scheduler tries `/internal/push/race-reminder` every minute inside the 19:55-20:00 window (the `race-soon:{date}` claim makes multiple attempts send once); if the worker missed the whole window, `/internal/batch/start` falls back to a `race-start:{date}` broadcast at 20:00 — never both. TTL 1 hour so a missed notification never arrives stale. (2) CS NEWSLETTER — no separate push surface: the EXISTING `/api/v1/admin/cs/broadcast` (Decision 081 betimail port, TEST/ALL) gains a `push` flag; when set, one push per broadcast job (`cs:{broadcastId}`) with title = mail subject goes out alongside the mail — TEST mode pushes only to the calling admin's own devices (`onlyUserId` filter). Push failures never affect mail results and vice versa. VAPID keys live in env (VAPID_PUBLIC_KEY / VAPID_PRIVATE_KEY / VAPID_SUBJECT — web AND worker services); transport is the `web-push` library behind an injectable interface so tests never touch the network. Delivery failures increment `fail_count` (auto-disable at 8); 404/410 disable immediately.

## Decision 085 (2026-07-11, Owner)

085. Purchase funnel redesign — buying lives ONLY on the marketplace page, as a three-act funnel: SHOWCASE → RESERVE → WAIT. The economy engine, queues and ledger flows are untouched (Decisions 010/011/012/043/051 immutable); this is a UX consolidation with additive API changes. (1) SINGLE ENTRY: every 「馬を迎える」 CTA (dashboard, stable, tasks) navigates to /market — the dashboard's one-tap instant session button (from the 2026-07-11 audit) is retired because it locked 177.16 USDT with no confirmation. (2) SHOWCASE SHELF: art cards = live MANUAL listings plus RECENT REAL MATCHES rendered as SOLD cards (including Day0 mints, labeled 新規発行) — the owner's original idea of decorative "unsellable SOLD OUT horses" was replaced with real settled transactions to avoid fabricated-display (おとり広告) risk; nothing on the shelf is fictional. (3) NO CHERRY-PICKING, HONESTLY: tapping any card explains that named-horse purchase does not exist because ALL trades settle in the nightly 20:00 first-come deterministic queue (Decision 012's fairness rule IS the plausible reason — no invented excuse), then funnels to the reserve panel. (4) RESERVE PANEL: shows the wallet's available USDT, explains the per-head lock (max 177.16, Day0 mint charge 102, difference auto-refunded), computes 最大N頭 = min(floor(available/177.16), 10 − pending sessions), and offers a head-count dropdown; N=0 flips the CTA to a deposit link (/wallet — chain deposits are not live yet, so the link is the whole dependency). Confirmation dialog states the total lock before executing. (5) MULTI-HEAD API: POST /api/v1/purchase gains optional `count` (1-10, default 1); the server derives per-session idempotency keys `{key}#i` from the request key so replays stay exact; response adds `session_ids`. (6) COMPLETION: on-screen guidance 「20:00のレース終了後、スマートマーケットプレイスシステムが購入予約を処理します。他ユーザーとのP2P取引、または新規発行馬の購入となります」+ a best-effort reservation-received email through the Decision 081 Resend wrapper (never blocks the purchase); the lock already appears in the wallet transaction history. (7) VOCABULARY: user-facing copy says 購入予約 everywhere (購入セッション is retired from the UI). GET /market/place's recent_matches gains dna_hash/rarity/is_mint so SOLD cards can render art.

## Decision 086 (2026-07-11, Owner)

086. Listing mode becomes an EXPLICIT USER CHOICE, and the buy side gains opt-in auto-reservation — closing the automation asymmetry (selling was automatic, re-buying manual) while STRENGTHENING the R1 posture: the user now demonstrably chooses how their horses are sold. (1) MANDATORY CHOICE: on first dashboard visit a blocking modal makes the user pick SMART (recommended) or MANUAL as their listing mode; no pre-checked default — until chosen, the user's horses are NEVER auto-listed. The comparison is honest facts only: SMART = the economy engine may list one of your horses per night at the ladder price, the horse KEEPS RACING while listed, hands-free, and unlocks auto-reservation; MANUAL = you pick the horse and moment yourself, but the listed horse sits out races (Market Lock, day/value frozen), operations are 1/horse/day, and auto-reservation is unavailable. The matching queue is NOT touched — Decision 012's listed_at-ASC order stays immutable; no source-based priority (a rigged queue would contradict the published fairness story and be visible in the ledger). (2) STORAGE: user_trade_settings (auto_list, auto_reserve — requires auto_list, auto_reserve_max 1-10 or NULL = MAX-as-balance-allows, default 1, chosen_at). Toggles surface in TWO places (dashboard AUTO tile + /market reserve area), changeable anytime. (3) ENGINE: profit-taking selection's eligible pool is restricted to horses of auto_list=true owners (deterministic sort unchanged, Decisions 015-017 amended accordingly); economic effect of opt-outs is more Day0 mints / fewer P2P listings, accepted — mint limits and listing rates stay owner-reviewable policy. (4) AUTO-RESERVATION: a post-batch worker job (NOT a new batch step — BATCH_STEPS_V1 untouched) creates purchase sessions for auto_reserve users right after the marketplace reopens, capped by min(setting, free session slots, balance/177.16), idempotent keys autoreserve:{date}:{user}#i, each run notifies in-app AND by email with a one-tap-off link (compliance guard against set-and-forget). (5) NOTIFICATIONS: three types join the Decision 065 set — HORSE_SOLD (seller finally learns their horse sold: name, net proceeds), AUTO_LISTED (the engine listed your horse tonight), AUTO_RESERVED (tonight's automatic reservation). Sold sellers also get a best-effort email via a post-batch sweep (mail_claims table for idempotency). (6) VISIBILITY: my_listings shows SMART listings too (source badge) — previously a smart-listed horse was invisible to its owner outside the shelf's MINE tag. Buyer-side cherry-picking of listed horses remains impossible (Decision 085 funnel), so demand cannot concentrate on high days; seller-side Day6 concentration is accepted as reserve-safe (parking analysis, Decision 076).

## Decision 087 (2026-07-11, Owner)

087. Two marketplace consistency rules. (1) UNSOLD SMART LISTINGS EXPIRE NIGHTLY: a smart-listed horse keeps racing, so carrying an unsold SMART listing across nights sells a Day-N horse at its stale listing-day price (seller loses). reopenMarketplace now cancels every still-LISTED SMART listing at batch end; the next night's profit-taking selection re-lists eligible horses at the CURRENT day price. Manual listings still carry over — the manually listed horse is frozen (Market Lock), so its price and day never diverge. (2) MARKET-LOCKED HORSES DON'T COUNT FOR SUPPORT-BONUS TIERS: Decision 077's org/direct tier volumes now exclude horses sitting in a live MANUAL listing. A parked horse bears no BURN risk; counting it would allow risk-free tier padding (park the team's horses, keep the tiers forever). SMART-listed horses keep counting — they keep racing and keep their risk. Owner confirmed both rules in the marketplace redesign review.

## Decision 088 (2026-07-11, Owner)

088. Training UX overhaul — make the fatigue tradeoff visible, add one-tap bulk training, and draw the free/paid line. NO full auto-training: the owner's own challenge ("if everyone auto-trains optimally, the feature is meaningless") is correct — races are relative, so universal optimal automation cancels itself out AND kills the daily-visit ritual that the 19:55 push notification feeds. The nightly training tap stays a deliberate player action. (1) VISIBLE MATH: the training picker becomes three radio cards showing the REAL public numbers per horse — tonight's score bonus from trainingModifierV1 (type affinity +3/+4/+5) and the net fatigue delta from CONDITION_FATIGUE_V1 (attack ≈ +8 fatigue/night, recovery ≈ −4), plus the LUCK-type note (any training widens the random range to −2..+4). The old copy hid that training's main effect is a direct score bonus and that fatigue compounds (it eats condition point-for-point). (2) RECOMMENDED CHOICE: recommendedTrainingV1(type, fatigue) — fatigue ≥ 60 → RECOVERY; SPRINTER → SPEED; POWER → POWER; ENDURANCE/BALANCED/LUCK → RECOVERY (dominant by the constants) — drives an おすすめ badge and pre-selection. (3) BULK TRAIN: POST /api/v1/horses/train-all applies the recommendation to every untrained ACTIVE horse (manual-listed and snapshot-frozen horses skipped; per-horse individual training done first is respected via on-conflict skip); no per-horse notifications (anti-spam — the response and trained badges carry the result); stable page gains the ⚡ button. "Hand-train the aces, bulk the rest" is the intended pattern. (4) FREE vs PAID: training is FREE (no item required) — the section headers now say so (今日の調教 無料·1日1回 / ブーストアイテム 任意·ショップ購入制) because even the owner briefly conflated the two systems; items are never auto-consumed by anything.

## Decision 089 (2026-07-13, Owner)

089. Admin fund grants: threshold-based approval, testnet funding source. (1) INSTANT SMALL GRANTS: grants of 1,000 USDT or less post immediately with a SINGLE active FINANCE_ADMIN/SUPER_ADMIN approver — the owner is right that forcing two admin logins for demo/debug test-USDT distribution only degrades the debug experience while the chain is Amoy testnet. This amends the constitution line "Admin adjustments require audit records and dual approval" with the same threshold philosophy already accepted for withdrawals (Decision 060-064: only large movements need two humans). The audit record remains mandatory and atomic in BOTH paths; the ledger layer re-enforces the cap and roles (postSingleApproverAdjustment), and the DB check constraint only allows self-approval at or below the 1,000 threshold. Grants above 1,000 USDT keep the full dual-approval flow (requester cannot approve, combined roles must cover FINANCE_ADMIN + SUPER_ADMIN). (2) FUNDING SOURCE (TESTNET INTERIM): grants draw from PLATFORM_DEPOSIT_CLEARING (negative-allowed by design; same posting as the original 2026-07-03 grant) instead of PLATFORM_OPERATING_RESERVE, whose 9.40 USDT balance would reject any meaningful grant. Mainnet migration MUST revert the source to the operating reserve — recorded in the HANDOVER.md launch checklist. Migration 20260713010000.

## Decision 090 (2026-07-13, Owner)

090. Operator root chain and default sponsorship for organic signups. (1) STRUCTURE: a vertical operator chain is created at the top of the placement tree using Gmail plus-alias structural nodes (login-incapable by design -- Google resolves +N aliases to the same account): goldbenchan+7 (root, tier 1) -> +6 -> +5 -> +4 -> +3 -> +2 -> goldbenchan@gmail.com (tier 7, the real operator account) -> kusanokiyoshi1 (tier 8) -> guri.baggio (tier 9). Applied by guarded migration 20260713020000 (no-op in test/fresh environments; idempotent; placement_audit trail; null->value writes are permitted by the immutability triggers). (2) DEFAULT SPONSORSHIP: signups WITHOUT a referral URL are automatically bound to the operator account (env DEFAULT_SPONSOR_EMAIL, default goldbenchan@gmail.com) as direct referrer AND immediately placed directly under it, with a placement_audit row. Consequence: an organic user'"'"'s BURN support bonuses (7 tiers up = the operator + the 6 alias nodes) are fully captured by the operator chain as a pool instead of partially remaining unpaid in the MLM reserve. Invited signups are unchanged: sponsor binds at provisioning, placement stays a manual sponsor action. Alias node balances are swept later via the dual-approved admin adjustment flow. User-facing org APIs remain downline-only (7 tiers); upline is never exposed to users.

## Decision 091 (2026-07-13, Owner)

091. Public documentation pages (/docs) — English only, white "API docs" visual register (owner's explicit reference: the OpenAI / Anthropic API documentation look), reachable from the landing footer's Docs link (previously a dead placeholder span). Context: the 2026-07-13 lawyer review returned an overall GO (not gambling / not multi-level marketing under the chain-sales law / not a collective investment scheme / player funds are held, never managed or invested / the product is a game — recorded in LEGAL_REVIEW_MEMO.md from the owner's verbal report; the review was oral, no written memo exists). EDITORIAL RULE: the pages publish FACTS ONLY — fund handling (deposits, 100% withdrawability before commitment, gas-at-cost withdrawal fees, book-entry P2P settlement, no investment/lending/staking of player funds, no yield, no currency exchange), fairness (deterministic engine, commit-reveal, immutable results, single fair queue), and risk disclosure (elimination = loss of purchase value, no promised returns, digital-asset risks). NO legal argumentation ("this is not X") appears on public pages — self-referencing legal claims can only hurt, and naming the avoided classifications would itself violate the honest-vocabulary rule (Decision 081's banned-words list; the referral program is only ever called サポートボーナス / support bonus). The "why this is not X" analysis lives in internal documents for lawyers, exchanges and investors (Legal Architecture, per the owner's GPT legal log 法務.txt). All published figures already appear in the product UI (mint 102 = 100 + 2 fee, 2% seller fee / 98% proceeds, max 177.16 lock per horse, 1,000-USDT withdrawal review threshold, gas-at-cost). Terms of Service / Privacy Policy remain to be drafted and must pass lawyer review of the actual wording before publication. Routes: /docs, /docs/funds, /docs/fairness, /docs/risk (public, no auth).

## Decision 092 (2026-07-14, Owner + second administrator)

092. Support bonus trigger moves from burns to CHAMPION CELEBRATIONS (お祝い金). Rationale: paying uplines when a downline horse burned made "your loss = my income" the structural story, contradicting the program's name and intent; the second administrator proposed paying when the organization produces a champion instead, and both administrators approved. The lawyer confirmed verbally (2026-07-14, owner-reported) that the change still does not constitute chain-sales (連鎖販売取引) and involves no 特定負担 — same assessment as before. MECHANICS: (1) FUNDING UNCHANGED — the pool is the existing PLATFORM_MLM_RESERVE, fed by the mint-time reserve allocation (5.40 per horse) plus burned horses' item costs (ITEM_SUPPORT_FUNDING, Decision 078). Burns still fund; they just no longer pay anyone directly. (2) PAYOUT — when a horse reaches DAY7_CLEARED, a queue row per tier (support_celebrations, unique horse_id x tier, settled rows immutable, delete-forbidden) is enqueued and paid FIFO (champion_date, created_at, horse_id, tier) from the pool, capped by the pool balance: tonight's champions pay inside step 17's composite, carried-over backlog drains in step 16 — BATCH_STEPS_V1 unchanged. Tier amounts (3/2/1x5 = 10 per champion), tier unlocking (Decision 077 org/direct volumes, evaluated point-in-time at the PAYING batch), ACTIVE-only recipients and no-roll-up UNCLAIMED semantics all carry over from Decision 074; unpaid money stays in the pool. Ledger reuses MLM_REWARD_PAYMENT with keys celeb:{horseId}:t{tier}, so /support totals keep working. New notification SUPPORT_CELEBRATION_PAID (「あなたの組織からチャンピオンが誕生しました。」). (3) SOLVENCY — Monte Carlo verified (SUPPORT_POOL_SIMULATION.md, engine-faithful floor rule + ADR-012 jitter, full-10-payout worst case): expected inflow 5.47 > outflow 4.53 per horse; zero shortfall across 20k trials at 30+ mints/night including a demand-stop scenario; the only weak spot is tiny populations (<10 horses/night, where floor() yields zero burns), which the FIFO carry-over absorbs by design. Migration 20260714010000. UI copy updated everywhere (landing TEAM section now celebrates champions — the BURN node became a CHAMPION node — /support lead/tier-meta/invite-note/history label, guide, items funding note, CS knowledge, daily-derby log tag CELEB); the honest-copy rules (no promises, サポートボーナス naming) are unchanged.

## Decision 093 (2026-07-14, Owner)

093. Tonight's field and burn slots become visible everywhere (honest "grand opening" advantage). Business context: the owner worried that a small early player base losing horses would churn (the pachinko grand-opening problem) — but this game must never touch the odds, secretly or otherwise. The answer is DISCLOSURE of an advantage the constitution already grants: burn count = floor(entrants x nightly rate), so small fields burn disproportionately few horses (9 or fewer entrants at typical rates -> zero burns; 13 entrants -> exactly 1). Early players demonstrably face better survival odds under the published rules. IMPLEMENTATION: (1) `tonight_field` on GET /daily-derby/status (shared, process-cached): entrants = ACTIVE horses minus live MANUAL listings — tonight's purchase reservations mint AFTER the race, so the number cannot be moved by last-minute buying; only manual listings shift it until batch lock. Burn-slot bounds use the fixed ADR-012 jitter envelope (8.0%..13.5%) via domain burnSlotRangeV1 — a true bound regardless of economy status or policy, and at small N the range collapses to a single "確定" number. (2) PADDOCK: a TONIGHT'S FIELD strip (entrants + BURN枠 + 確定/全馬生還の夜 tag + the floor-rule footnote 「対象馬は発走まで誰にも分かりません」). (3) LANDING: the gate card gains a live-data line (出走予定/BURN枠, LIVE DATA pulse), fed by a direct public DB read that fails soft. Copy is strictly factual — a slot ceiling, never a per-horse implication, no profit language (R1 rules). The companion launch ideas (mint-fee-zero campaign, the lawyer-approved 見舞い voucher) remain open items pending owner go.

## Decision 094 (2026-07-14, Owner)

094. Horse transfers between users (in-game asset gifting). Legal frame: user-to-user USDT transfers remain nonexistent; horses move as GAME ASSETS whose USDT outcome stays genuinely uncertain. The cash-out loophole (transfer -> manual listing under Market Lock -> near-certain sale = quasi-remittance) is closed by marking gifted horses permanently (horses.gifted_at): a gifted horse can NEVER be manually listed. It is deliberately NOT excluded from smart-listing selection — the owner correctly rejected a full sale ban because exempting gifted horses from the engine's profit-taking would spawn a mass "gift each other to dodge sales" meta; gifted horses stay in the pool like everyone else's. Net result: a gifted horse's only USDT paths are race outcomes (champion/burn) or engine-selected smart sales — neither controllable by the holder, both carrying nightly burn risk. MECHANICS: POST /horses/{id}/transfer rides the Decision 079 user_transfers rails (asset_type HORSE + horse_id column); email addressing, ACTIVE recipients only, instant and irrevocable, marketplace OPEN only (so ownership at tonight's snapshot is unambiguous), sender cap 3/24h (separate from the item cap; horse rows also count toward the flat 20), one transfer per horse per day via idempotency key horse-gift:{id}:{date} (also blocks same-day relay chains), listed horses (any source) not transferable. Recipient notification HORSE_GIFT_RECEIVED (「馬が届きました。今夜からあなたの厩舎で出走します。」). Training/committed items travel with the horse; org-volume attribution follows ownership (the recipient bears the burn risk — consistent with Decision 087). UI: transfer form with hard warnings on the horse detail page, 贈られた馬 badge, gifted horses excluded from the manual-listing picker. Migration 20260714020000.

## Decision 095 (2026-07-14, Owner — Malaysian principal's seminar funnel)

095. Seminar promo horses (casino-bonus-chip onboarding). During the initial season, seminar attendees receive ONE free horse instead of being asked to deposit USDT. The Decision 094 gifted-horse semantics make this a true bonus chip: the horse cannot be manually listed (no cash-out of the chip itself), races every night, and pays out only through real outcomes — champion 200 USDT is real winnings, a burn costs the attendee nothing. SUPPLY: the promo stable (env PROMO_STABLE_EMAIL, default goldbenchan@gmail.com) buys stock through the NORMAL nightly purchase queue — no special mint path, economy and coverage gate untouched; horses born at 20:00 are distributable from the NEXT day (10 reservation slots/night, so staff stock up in advance). Unclaimed stock keeps racing under the stable and self-liquidates (Day7 clear returns 200 vs the 102 cost; burns are the real promo expense). ALLOCATION: youngest current_day first — new users get the longest 7-day journey; older stock stays with the stable. DISTRIBUTION: (1) admin direct gift (POST /admin/promo/gift, any admin role, audited, exempt from the 094 3/day sender cap, one per recipient per day) for on-site staff; (2) redeem codes (promo_codes table): admins mint batches (SDD-XXXX-XXXX, ambiguity-free alphabet, optional expiry, CSV export), attendees self-redeem at the dashboard; one redemption per user per campaign (partial unique index), single-use codes, marketplace OPEN only. Admin console gains /admin/promo (stock count, stocking runbook, code generation/listing, direct gift); the dashboard gains a quiet gold redeem toggle. Anti-farming: distribution is human-controlled (codes/staff), capped per person, and the 094 no-manual-listing mark blocks the free-USDT-faucet path. Lawyer follow-up noted: confirm the ~100-USDT entry-gift classification (same thread as the approved 見舞い voucher). Migration 20260714030000.

## Decision 096 (2026-07-14, Owner)

096. The 10-concurrent-purchase-session cap (Decision 051) is lifted for everyone. Rationale: real buyers exist at the 10,000-USDT scale (~56 horses) and the promo stable needs bulk stocking (Decision 095); the cap was a UX/abuse guard, not an economic invariant — the true constraints are the balance lock (177.16 per session), the mint coverage gate, and the 10,000/night mint limit, all untouched. MAX_CONCURRENT_PURCHASE_SESSIONS becomes 1000, explicitly a runaway-bug ceiling rather than a product limit; a new PURCHASE_MAX_PER_REQUEST (100) bounds one API call because sessions are created serially (a whale reserves 56 in one click; 500 takes five clicks). The reserve panel caps its dropdown at 100 per operation and drops the "10件まで" copy; the auto-reserve setting keeps its 1-10/MAX shape (MAX now means balance-bounded, as documented). CS knowledge updated.

## Decision 097 (2026-07-14, Owner)

097. Stable names — the player's public identity. Horses churn daily through P2P but the STABLE persists; the owner wants the stable, not the account, to be the face of the player. users.stable_name: 2-20 chars (Japanese/latin/digits/spaces; no URLs/symbols/@), unique case-insensitively, one change per day, clearable to null; admins can force-clear with an audited reason (moderation). SURFACES: (1) the my-stable page title and the dashboard stable tile show the name instead of マイ厩舎; (2) matching counterparts (night-results sold/bought, champions hall) and gift/transfer sender names prefer the stable name; (3) the org map/pool/member views show 「厩舎名(ab***@domain)」 — emails stay MASKED at every tier including direct referrals (owner's explicit call: upline placement can put strangers in your downline, and address exchange for gifting happens off-platform via LINE/Telegram, which is lower-risk than exposing addresses in-app); transfers remain email-addressed. Unset names fall back to today's masked display everywhere. Migration 20260714040000.

## Decision 098 (2026-07-14, Owner)

098. Maintenance mode. An operational switch (system_settings key 'maintenance', service-only table): while ON, every non-admin request to the web app — pages and APIs alike — is blocked; visitors see a maintenance screen (operator-set message, funds-are-safe note, and a small admin-login entry so an admin with an expired session can still get in). Admins (active admin_role_grants) retain FULL access: login, every page, every API. Enforcement lives at the web bridge (503 MAINTENANCE for non-admin dispatch) plus the root layout (screen swap); the flag is process-cached 10s and FAILS OPEN if unreadable. Workers/internal auth and /healthz are exempt by construction — the 20:00 MYT batch keeps running during maintenance (stopping settlement is a separate decision, never implied by this switch). Toggling is admin-API-only and audited (MAINTENANCE_ENABLED/DISABLED, singleton reference UUID 0000…0000). Cockpit toggle on /admin with a confirm dialog; admins browsing during maintenance see a persistent banner. Migration 20260714050000.

## Decision 099 (2026-07-14, Owner — after joint simulation review)

099. Starter rate — the tier-1 celebration amount scales smoothly with the recipient's org size. Context: under Decision 092 amounts (3/2/1x5) a grassroots organizer needed ~4x the organization of the pre-074 era to break even ("the game will not grow — I would not play it" — owner). Fixed reweights toward tier 1 were simulated and REJECTED: they crush depth-heavy leaders (whose orgs grow at tiers 2-4 and who sit at 96% positive under current amounts). Adopted instead: tier-1 amount = clamp(150000 / recipient's ORG volume, 3.00, 8.00), 2dp half-up in cents math — 8.00 while the org is small, smoothly declining to the old 3.00 at 50,000 volume. The curve keeps rate x volume constant, so tier-1 income NEVER decreases as an org grows (a stepped 8/5/3 table was rejected too: it drops a flat org's income 29% at the 20k crossing). Tiers 2-7 amounts, all unlock thresholds, the champion trigger, and the pool/FIFO machinery are UNCHANGED — nobody earns less than under 092, and depth leaders are untouched. The amount is FIXED on the champion night from the tier-1 ancestor's org volume (same nightly yardstick as tier unlocks; queue rows are immutable so carryovers keep that night's rate; absent ancestors enqueue at the 3.00 floor). Pool safety verified across launch/growth/mature synthetic populations (worst 4.10 per mint vs the 5.40 reserve, leaf-parents-stay-at-8 effect included) and promotion-cliff-free growth verified across flat/balanced/deep trajectories — PLAYER_EV_SIMULATION.md §4.8-4.9. UI: /support leads with a STARTER RATE gauge (8.00 -> 3.00 journey, boost pill, the never-decreases guarantee line); GET /support/summary gains starter_rate. Lawyer follow-up: one-line confirmation that recipient-scaled amounts within the existing volume-qualification family raise no new issue (bundle with the 092 written confirmation).

## Decision 100 (2026-07-15, Owner)

100. Buyer queue is time-independent. The nightly purchase-assignment buyer queue was ordered `created_at ASC -> purchase tiebreak DESC -> session_uuid ASC` (first-come). Because buyers cannot choose P2P-vs-mint (assignment is automatic: P2P-first, then Day0 mint for the shortfall), reservation TIMING was the only lever a user had — and since the earliest reservers get the scarce P2P listings while later reservers fall through to fresh Day0 mints (cheaper, full 7-day runway), it created a perverse incentive to reserve at the last second before the cutoff. New rule: `purchase tiebreak DESC -> session_uuid ASC` only — the queue is a purely seed-derived deterministic lottery (from batch_run_id × session_uuid × algorithm version), so **when you reserve no longer affects your position**; everyone is shuffled fairly by the seed and anyone can re-verify. The horse/seller queue keeps `listed_at ASC` (listing is mostly automatic, little user timing leverage). Determinism, replayability, and the "no VIP/referral/balance/AI/admin preference" invariant are unchanged; assignment_algorithm_version was NOT bumped (the change is forward-only — past batches are immutable and no replay verifies assignment order). `packages/settlement-engine/src/assignment/queues.ts` (buildBuyerQueue) + assignment.test.ts.

## Decision 101 (2026-07-17, Owner — FUN overhaul, "V2")

101. Total Value engine and rarity retirement (race engine V2). Every horse is described by ONE number, Total Value 0-100. V2 score = total_value + conditions modifier (prepared-for weather/track/surface, +-4) + luck (+-3, Irwin-Hall). Skill sharpness deliberately stays SHARP (owner's explicit call after simulation: in a mixed population, casual players reach champion ~24% vs ~93% for perfect play — an esports positioning; the rookie-bracket softener is kept as a bolt-on option if churn data demands it). New mints roll Total Value uniformly in 40-75 — the 90s can only be TRAINED into existence, never born. Value decays 2.0 per race (neglect shows) and gains above 85 are halved (soft cap keeps the 90s special). Rarity (COMMON..LEGENDARY) is retired entirely — probability and modifier tables deleted; horse TYPE survives as the play-style axis (training affinity, weather/track aptitude, hidden preferences; LUCK's widened trained random range stays). Condition and fatigue disappear as separate stats, folded into Total Value (internal state variables are an implementation choice; the display is one number). Honesty duty: onboarding, guide and CS knowledge must say plainly that skilled play wins, and the safety-band badge (top 40% SAFE / bottom 25% RISK) is always labeled a guide, never a guarantee. Deterministic, commit-reveal verifiable as ever. Model sim + real-engine RTP reconciliation recorded in FUN_V2_PLAN.md s5.5.

## Decision 102 (2026-07-17, Owner)

102. Two real races a day on the LV ladder. "Seven Days" is reframed as seven STAGES: LV.1-7, one survived race = one LV (all DAY displays replaced); LV7 clearance = champion, fastest journey 3.5 calendar days. Race times (owner's final call, superseding an earlier same-day 19:00 MYT idea): morning 8:00 MYT (9:00 JST) and night 20:00 MYT (21:00 JST) — the night race keeps its current time, so V2 only ADDS the morning race. batch_runs becomes keyed by (batch_date, slot MORNING|NIGHT); idempotency keys, records, ledger references, my-results, transparency, forecasts and the live show all become per-race. The price table keeps its values re-read as LV0..LV6 (100..177.16, buyback 200); the burn-rate vessel (8.0-13.5% per race) is unchanged — per-line math is cadence-independent (verified by simulation), only velocity doubles. Champion rewards stay split over 7 DAYS (not 7 races) — owner's call, far safer for buyback-reserve cash flow. Training/harvest/ticket runs once per race (two cycles a day). NEW: buyback-reserve backstop — discovered when the real-engine drill drove the reserve negative (jitter pushed a cohort past the 46.8% break-even): immediately before PAY_DUE_BUYBACKS, if the reserve is short of that batch's due payments, an explicit ledger transfer tops it up from the operating reserve as a REGULAR batch step, making unpaid buybacks structurally impossible. Steady-state planning figure: ~392 mints/day sustain a 1,000-horse field.

## Decision 103 (2026-07-17, Owner)

103. Pool purchase — "build me a $1,000 stable" — replaces the per-horse 177.16 lock. Users reserve an AMOUNT (package badges 200/500/1000 (~7-9 horses)/2000/5000/10000 USDT plus free input); the whole amount locks, editable/cancelable until cutoff. At the next race (morning or night — two chances a day) the engine allocates P2P listings first (Decision 100's deterministic lottery order preserved) then LV0 mints (102) until the budget is exhausted; the remainder is always below 102 (less than one cheapest horse) and auto-returns. A listing the current buyer can no longer afford is SKIPPED to the next buyer in the queue (listings never lose their chance to sell); the buyer fills up with mints. Prices stay on the fixed public table — no dynamic pricing (the legal shield stands). The old per-session lock, concurrent-session cap and auto_reserve are retired; auto-reserve is redefined as "auto pool-reserve N USDT from balance each race". The personal show closes with "YOUR NEW STABLE — your 1,000 became 8 horses", total values included (the same-price-different-horse treasure hunt).

## Decision 104 (2026-07-17, Owner)

104. Training system V2 — menus, hidden preferences, tickets. Six menus with PUBLIC ranges, up to two per race cycle (repeats allowed): HILL/slope +1..+5, POOL 0..+3 (safe, no downside), SPAR/sparring -2..+6 (high risk), GATE 0..+4, WOOD chip +1..+4, REST 0 (negates one decay tick, preference-agnostic). Exact ranges get final tuning so aggregate rolls reproduce the simulated informed/miss distributions. Hidden preferences are derived deterministically from dna_hash: each horse has one favorite menu (rolls skew high) and one disliked menu (skew low, wider downside). The LAW is type-correlated at 70% (e.g. most SPRINTERs love HILL or GATE — learnable meta) with 30% individual exceptions (discoverable only by trying that horse); combos containing the favorite earn a synergy bonus; everything is hash-committed and revealable later (cheating is disprovable). The roll RESOLVES on the confirm button — previews show only public ranges; redo until snapshot; ticket and harvest fire on first confirm only (the A2 pattern). Paid items are rebuilt as two classes: TRAINING items act directly on Total Value (bigger ranges, little/no downside — the "transform your horse" spend) and RACE items are preparation bets on the 70%-accuracy forecast (+4..+8 equivalent when right, -7..-3 when wrong) — preparation, never interference or wagering (R1 intact). The item catalog is REBUILT from scratch for the two classes (owner's call) — the legacy 35 items retire at the mainnet reset; new catalog and art to be drafted for owner approval.

## Decision 105 (2026-07-17, Owner)

105. Marketing budget account, ratified. PLATFORM_MARKETING_BUDGET ledger account and the /admin/marketing tab (FUND/RETURN book-entry moves from/to the operating reserve; <=1,000 USDT instant with one FINANCE/SUPER admin, larger amounts dual-approved by a different admin; every move audited) — implemented and live 2026-07-17 (migrations 20260717010000/020000). Principles: race math and prices are never touched — this account only ADDS prize events; users never see its balance or budget; every event is explicitly time-limited; no withdrawal or on-chain path exists from this account. Each payout route (jackpot etc.) is added individually with its own decision. STANDING OWNER INSTRUCTION recorded here: lawyer consultations are human-to-human and gate PUBLIC RELEASE only — development never halts waiting for legal answers.

## Decision 106 (2026-07-17, Owner)

106. Weekly jackpot — built now, released later. Implemented as part of V2 and LIVE ON TESTNET during the trial season; whether it goes to mainnet production is the owner's call after the lawyer's confirmation (per the standing instruction in 105). Training tickets are the lottery entries (count-proportional — big stables are favored, owner-approved); one draw a week, announced in the night show's final act; the draw uses the same commit-reveal machinery as races (publicly verifiable). Prizes are USDT funded exclusively from the marketing budget account (its balance is the structural ceiling); winners are announced masked. Testnet placeholder parameters (all runtime-configurable): weekly, 100 USDT, single winner, tickets reset weekly. Production prize size, winner structure and ticket expiry are decided by the owner at release time.

## Decision 107 (2026-07-17, Owner)

107. V2 training roll is FINAL on first confirm — no redo. Supersedes the "redo until snapshot" clause of Decision 104 for V2 rolled training only. Rationale: if a redo re-rolls (or lets the player probe the deterministic roll of every menu combination and keep the best), the downside risk becomes fake and the simulated progression curve of Decision 101 (one roll per race cycle) no longer holds; the confirm button must be the real gamble moment. Rules: one training confirm per horse per race cycle (date x slot), resolved at confirm time from a deterministic seed, immutable and undeletable afterwards (DB-guarded); the misclick guard is the existing confirm dialog plus public-range preview; the A2 "ticket fires on first confirm only" clause becomes trivially "every confirm" since every confirm is the first. The V1 production season's rollless redo (delete+insert, A2) is untouched — it retires with V1 at the mainnet reset.

## Decision 108 (2026-07-18, Owner)

108. Weekly jackpot draw details (completes Decision 106 for implementation). Week = the race cycles from Monday MORNING through Sunday NIGHT (MYT); a ticket is a training confirm attributed to a cycle inside that week by its (effective_race_date, slot) — deterministic and replay-verifiable, independent of the wall-clock confirm time. The draw resolves in the Sunday NIGHT batch and is announced in that night show's final act. If the marketing budget balance is below the configured prize at draw time, that week's draw is cancelled (no payout movement, admin alert, no jackpot act in the show); tickets still reset weekly. A week with zero tickets is void: the prize does not carry over — next week's prize is the configured amount as usual and no money leaves the marketing budget. No account exclusions: every user with tickets is eligible, including admin accounts (the testnet trial runs with staff accounts; revisit at production release per 106).

## Open Items for Implementation Phase

These are implementation artifacts, not business rule gaps:

- exact SQL DDL
- Prisma or SQL migration implementation
- OpenAPI generation
- DTO and validation schema implementation
- frontend UX copy
- deployment scripts
- 100,000 user simulation implementation
