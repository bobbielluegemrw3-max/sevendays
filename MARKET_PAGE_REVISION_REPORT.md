# マーケットプレイス（/market）再デザイン 完了報告（実装側 → レビュー側）

**日付**: 2026-07-24
**対象**: `/dev/market-preview`（プレビュー・**オーナー実機承認済み**）＋ 手放し5%精算機構（休眠・本番投入済み）
**正典**: `MARKET_PAGE_IDENTITY_REVISION_SPEC.md`（レビュー側）＋ デザイナー正典 `Marketplace.html`（見た目の厳密基準）
**手数料検証**: `RACE_PRE_EXIT_DISPOSAL_SIM_REPORT.md`（B=安全・推奨5%）
**prod**: `000172a`（`/healthz` 一致）

> **一行**: 「厩舎の窓口」は**撤回**し表示名を「**マーケットプレイス**」に。デザイナー正典 `Marketplace.html`（3幕）を**インラインスタイル値を厳守して忠実写経**し `/dev/market-preview` に構築 → **オーナー実機承認**。経済機構には一切触れていない（視覚プレビューのみ）。手放し5%の裏機構は**休眠かつスキーマ非依存**で既に本番投入済み（`a677d39`・現状バイト等価）。**本番 `/market` への結線は未実施**＝レビュー側の [CODE] 再照合を待って着手したい。

---

## 1. 何を作ったか（`000172a`）

- **表示名**: 「厩舎の窓口」→ **「マーケットプレイス」**（センスの都合でオーナー撤回）。URL `/market`・ナビ・API は据え置き。
- **`apps/web/app/dev/market-preview/MarketplacePreview.tsx`**（新規・client）＋ `page.tsx`（差し替え・旧 fixture プレビューを置換）。
- 3幕構成を正典どおり写経：
  - **ACT 1 迎える**（`.primary`=「予約する」1つ）：シネマティックなヘッダー・ゲート（ブランドロックアップ／巨大カウントダウン／mono ステータス・ティッカー）＋プール予約（残高・プリセット5＋全額・自由入力）＋出走予定8頭グリッド。
  - **ACT 2 手放す**（従・neutral）：受取（−5%）↔ 走り続ければ（+10%/日・チャンピオン200）の**トレードオフ決断カード**＋手放し待ち（SMART/手動バッジ・Market Lock 注記・戻す）＋**手放し確認モーダル**（手数料5%表示・チェック必須）。
  - **ACT 3 清算**：実成約のみ（SOLD／新規発行）を1本に統合。
- **回転する光の枠線**（仕様 §2-1 の目玉）：出走予定カードに `@property --featAngle` を回す `conic-gradient` の1pxリング（`featSpin`）を `::before` で再現（`HorseV3Grid` 聖杯枠と同技法）。
- PC/モバイル切替・カウントダウン（毎秒）・トースト・手放しフロー（mine→listed→戻す）を再現。

**忠実性の担保**: 正典のインラインスタイル文字列を**そのまま保持**（ハンドコンバートによるドリフトを排除）。`:root` トークンはラッパー要素に載せグローバル汚染を回避、keyframes/`@property`/reduced-motion のみ `.mp-scope` スコープの `<style>` に。

---

## 2. 受け入れ基準（正典 §4）自己チェック

| 基準 | 状態 | 備考 |
|---|---|---|
| `.primary` は「予約する」1つ・赤は BURN/危険のみ・装飾トリオ無し | ✅ | 手放す＝neutral 枠線／戻す・キャンセル＝`.secondary`(マゼンタ枠)。ボタンに赤なし |
| 棚タップで「指名購入できません」を毎回出さない | ✅ | 棚は非クリックのプレビュー表示（指名導線を作らない旨を明記） |
| 成約情報はページ内1箇所（ACT3）に集約 | ✅ | 清算フィードのみ |
| 予約は残高からの予算入力＋パッケージ＋確認 | ✅（写経） | 本番結線時に実 `PoolReservePanel`＋本番 `POOL_PACKAGES_V2` へ差し替え（§3） |
| 手放しカードに受取（−5%）と諦める（+10%/日・チャンピオン200）が見える | ✅ | 2カラムのトレードオフ＋価値推移バー（発行100→現在→走破200） |
| 機構不変（予約/出品/取下/割当/精算 API・価格ラダー・Market Lock） | ✅ | プレビューは視覚のみ・コードで機構に触れていない |
| レイアウト安定・横スクロール無し・reduced-motion で光アニメ停止 | ✅ | `@media (prefers-reduced-motion:reduce)` で全 animation 停止 |

コピー規範（R1/R3）：数値は実数（ラダー・手数料・残高）、投機を煽る価格ティッカーなし、清算フィードは実成約のみ、「指名購入はありません — 一斉マッチング（先着順）で公平に成立」を明示。プレイヤー向けでない語（焼かれる／現金化 等）は不使用。

---

## 3. 手放し5%の裏機構（休眠・本番投入済み `a677d39`）

正典 §4-3 のとおり **`PRE_RACE_RELEASE_FEE_RATE = '0.05'`** を新設（既存 `P2P_FEE_RATE` 2% は不変）。既存MANUAL出品と同一キュー・同一精算を通し、fee適用箇所だけ経路で分岐できる形。

- `assignmentSettlement` に任意 `feeSplitRate`（既定=2%・`proceeds=price−2×feeHalf` で常に台帳バランス）。
- `market_listings.pre_race_release` **休眠列**（default false）マイグレーションを配置（**本番DB未適用**）。
- 5%精算のユニットテスト（133.10→受取 126.445・split 2.5%×2・clearing 0）PASS。
- **★execute.ts は意図的に列を読まず既定2%＝バイト等価**（本番DB接続不可のためデプロイ順序事故を避けオーナー判断で列読み取りを先送り）。

経済安全性は `RACE_PRE_EXIT_DISPOSAL_SIM_REPORT.md` で B（移転＋手数料5%）= A/G/H/I 全PASS（BURN10.7%・EVバンド内・母集団保存・外部原資ゼロ）を確認済み。

---

## 4. 本番結線（未実施・レビュー再照合後に着手したい）

正典 §6 の「開発側 → 承認後に本番結線」。**3点を同時に**入れる：
1. 休眠マイグレーションを**本番DBへ適用**（`supabase db push`・追加列は現行コードが無視するので先行適用は安全）。
2. `execute.ts` に列JOIN＋`release?PRE_RACE_RELEASE_FEE_SPLIT_RATE:P2P_FEE_SPLIT_RATE` の分岐を追加。
3. 承認済みの見た目を本番 `/market`（`MarketPlaceView`）へ結線：ACT1=実 `PoolReservePanel`（`POST /purchase {amount}`）＋本番 `POOL_PACKAGES_V2`、棚=`NftHorseArt`+`deriveNftLook`、ACT2 出品=`POST /market/list`（release フラグ）、取下=`POST /market/unlist`、ACT3=`recent_matches`、残高=`GET /wallet`。手放しAPIに release 経路フラグを渡す。

いずれも既存機構の再利用のみ（キュー・割当・価格ラダー・BURNフロア・Market Lock 不変）。

---

## 5. レビュー側にお願いしたい [CODE] 再照合（正典 §6-5）

本番結線に着手する前に、以下を確認いただきたい：
- **機構不変**：予約/出品/取下/割当/精算 API・価格ラダー・Market Lock に手を入れない結線プランで妥当か。
- **§9色 / R1**：`.primary` 1つ・赤は BURN 専用・装飾トリオ無し・投機表示なし、の情報設計で問題ないか。
- **5%/凍結**：手放し経路のみ 5%（既存2%不変）・出品中は day/価値凍結（Market Lock）で B の安全性前提（母集団保存）が崩れないか。
- **手放しAPIの設計**：release フラグを `POST /market/list` に足すか、別エンドポイントにするか（機構は同一キュー・同一精算のまま）。

---

## 6. コミット一覧（レビュー用）

```
000172a feat(dev)    /dev/market-preview を3幕マーケットプレイスへ写経（本報告の対象・承認済み）
a677d39 feat(market) 手放し5%精算機構を先行実装（休眠・スキーマ非依存・execute.tsは未結線）
83b9b4e docs(economy) レース前退出の経済安全性シム＋検証レポート（B=安全・A=不可）
```

---

## 7. 一目で現状

```
✅ /dev/market-preview = 3幕マーケットプレイスを正典どおり忠実写経（回転枠線・手放し5%表示・モーダル）
✅ オーナー実機承認済み（prod 000172a・管理者限定・公開404）
✅ 手放し5%裏機構 = 休眠かつスキーマ非依存で本番投入済み（現状バイト等価・execute.ts未結線）
📐 次 = レビュー側の [CODE] 再照合 → 承認後に本番 /market 結線（3点同時：マイグレ適用+execute.ts分岐+UI/API）
🚫 着手前 = 本番 /market・経済機構には未着手（盲目プッシュ禁止・機構不変）
```
```
prod=000172a ／ 未プッシュなし ／ 本番 /market は旧デザインのまま（結線待ち）
```
