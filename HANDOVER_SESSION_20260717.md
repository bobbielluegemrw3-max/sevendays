# セッション引継ぎ 2026-07-16夜〜07-17(FUN大改修の始動: 合意→A層本番→V2着工)

> 前セッション: `HANDOVER_SESSION_20260716.md`(調教表示・AppSelect・i18nバンドル)。
> 正典の序列: `HANDOVER.md` → 本書 → **FUN改修は `FUN_V2_PLAN.md`(§9=進行状態・§8.5=残作業)が最上位** →
> `FUN_REVISION.md`(合意の経緯)/ `FUN_V2_DECISIONS.md`(Decision草案の経緯)→ `docs/10_DECISION_LOG.md`。
> **Decision Logは106まで起票済み・次は107。**
> 本セッションの主要コミット: 7/16夜 `c145162`(perf)→`c639d24`(審判実結線)→`7e58659`(リプレイ)→
> `a65def1`→`0bae8ad`(脚切れ)→`fbf3053`(i18n/horses)/ 7/17 `2eb1805`(計画書)→タグ**`pre-fun-overhaul`**→
> `515d3fe`(A1)→`cca70f8`(A2)→`a1a3a50`(棚TV+チケット)→`47c1564`(広告費)→`cf1c478`(シム)→
> `4b23d88`(Decision起票)→`f3dc4cf`(エンジンV2純関数層)

## 0. 現在地(最重要)

- **FUN大改修が確定・進行中**。トリガー: セミナー約10名全員の「面白くない」評価(7/16)。
  5項目すべてオーナーと合意済み → **Decision 101〜106 正式起票済み** → **V2実装フェーズ**
- **本番稼働中の新機能(A層+B層)**: A1 総合値0-100+今夜の安全圏 / A2 調教=収穫の儀式
  (未回収→確定→回収+チケット・やり直し可)/ マーケット棚の総合値チップ / チケットカウンター /
  広告費口座+`/admin/marketing`(本番マイグレーション適用済み)
- **次の一手 = V2実装-1b**(§3参照)。リバートは `git reset --hard pre-fun-overhaul`

## 1. 7/16夜の部(FUN改修前の修繕)

1. **スケルトン体感の根治(`c145162`)**: 原因は往復回数×RTT(55ms)。GET /horses 9往復→2
   (隠しルック7クエリをUNION ALL 1本に・バッチ日確認をCTE化)/GET /wallet 5→1/ページ層のPromise.all化。
   ダッシュボード 12→4往復。**セッションプーラー上限=15実測**(EMAXCONNSESSION)→ pgのidle保持は60秒
   (`SCALING_PLAYBOOK.md` §D/B-3に全記録)
2. **審判演出が本番で一度も発火していなかった重大バグ(`c639d24`)**: 濁流のフィクション行と実馬名の
   偶然一致で発火する設計だった(偶然一致で誤結果表示すらあり得た)。status APIに `my_events`
   (当夜の実結果・FINALIZED後・1往復UNION ALL)を追加し、クライアントはセクション時間帯に
   スケジュール発火する方式に全面変更。点呼はBURN馬込み1周+スロット同期審判
3. **見逃しリプレイ(`7e58659`)**: ショー後〜MYT日付が変わるまで、未視聴ユーザーに1回だけ自動再生
   (localStorage判定・REPLAYバー+スキップ・`?replay=1`でQA)。リプレイバーはステージ枠の外に置く
   (`a65def1` — SOUND ONと衝突した実障害)
4. 小修正: ナビLIVEバッジ+60秒で消灯 / バフ`+4.00000000`のはみ出し / **厩舎馬アートの脚切れ**
   (`0bae8ad` — 7/15の隠し演出ラッパー`artWrap`に width/height 100% が無く canvas の基準が消えた)
5. **/horses+馬詳細の5言語化(`fbf3053`)**: ~150キー×5言語。**教訓**: クライアント境界下の
   コンポーネントに `APP_COPY.ja.xxx` をデフォルト引数で持たせると当該セクションだけツリーシェイクで
   チャンクに漏れる(21KB実測)→ インライン定数フォールバックにする(`RarityLegend`参照)。
   検証は `.next/static/chunks` を新規文字列でgrep

## 2. 7/17 FUN大改修(本編)

### 合意と計画(すべて文書化済み — ここでは所在のみ)
- 生フィードバック5本と triage → `FUN_REVISION.md` §6 / 合意済み設計 §7(調教)・§8(総合値)・
  §9(2レースLV+広告費)・§10(プール購入)
- 設計・計画書 = `FUN_V2_PLAN.md`(§0ゴール・§1不変条項・§3 A層詳細・§5 V2設計+シム結果・
  §8 Decision予定・**§9 進行状態チェックリスト**)
- **Decision 101〜106 起票済み**(`docs/10_DECISION_LOG.md` 英語・7/17)。要点:
  101=総合値エンジン(切れ味**鋭いまま**=eスポーツ路線・ミント40-75・減衰2.0・ソフトキャップ85・
  レアリティ廃止/タイプ存続)/102=朝8:00 MYT+**夜20:00 MYTのまま**(19:00前倒しは上書き廃止)・
  LV.1〜7・報酬は現行7日分割・**買戻し準備金バックストップ**/103=プール購入「◯◯$厩舎」
  (パッケージ200〜10000$・余り<102$・予算超過出品はスキップ)/104=調教6メニュー2組合せ・
  隠れた好み70/30・確定時ロール・**アイテムカタログは新規作り直し**/105=広告費追認/
  106=ジャックポット(テストネット仮値: 週100 USDT・1名・週次リセット)

### 本番投入済みの実装
- **A1(`515d3fe`)**: `race-engine/total-value.ts` totalValueV0(実エンジン計算から導出・突合テスト)/
  GET /horses・/horses/:id に total_value/tonight_rank/tonight_band(分布60秒キャッシュ)/
  厩舎カード・馬詳細のレアリティ表示撤去→総合値+安全圏
- **A2(`cca70f8`)**: 調教やり直し=**delete+insert**(スキーマ憲法どおり・UPDATE禁止トリガーあり)・
  チケット/通知は初回のみ(xmaxでなく事前SELECTで判定)・未回収バッジ・総資産の「−未回収」項
  (等式の数学を崩さない)・回収/チケット演出・戦績の帰属チップ・/me に training_tickets
- **棚TV+チケット+一括回収(`a1a3a50`)** / **広告費口座(`47c1564`)**: migration
  20260717010000/020000(enum追加は別ファイル先行=buff_status前例)・fund-grantと同じ閾値承認
  (≤1,000即時/超は二重承認)・**払い出し経路は未実装**(意図的)
- **エンジンV2純関数層(`f3dc4cf`)**: `domain/v2.ts`(全V2定数の単一ソース)+`race-engine/src/v2/`
  (mint・hiddenPreferences(70/30を統計テストで実証)・resolveTrainingRoll・softcap/decay・computeScoreV2)

### シミュレーション(結果は `FUN_V2_PLAN.md` §5.5.1/§5.5.2)
- モデルシム `packages/settlement-engine/scripts/fun-v2-model-sim.mjs`(シード固定):
  ケイデンス非依存・必要ミント2倍(392頭/日 per 1000頭)・50→89物語成立・安全圏誠実
- 実エンジンRTP突合: チャンピオン率47.7%(モデル45.1%と整合)・運営2.78%(最保守)・
  **買戻し準備金のジッター枯渇を発見**(→102-8バックストップに反映)

## 3. 次セッションの最初の一手: V2実装-1b(settlement結線)

`FUN_V2_PLAN.md` §9の「V2実装-1b」から。やること:
1. **マイグレーション**: `horses.total_value numeric`(+初期値はリセット時にmintTotalValueV2)・
   スナップショットのV2列(total_value/prep/luck凍結)・`race_engine_version 'race_v2.0'`。
   enum追加が要る場合はファイル分割(コミット後でないと使えない — buff/marketing前例)
2. **snapshots.ts**: V1の`computeDailyState`+`baseAbilityScore`経路 → V2では total_value の
   漸化(調教ロール適用済み値+減衰)を凍結。調教はV2では**確定時に既にロール済み**なので、
   スナップショットは値を読むだけになる設計が素直(ロール結果の保存先=training_sessions拡張
   (メニュー2つ・perMenu・synergy・delta列)が先)
3. **scores.ts/production.ts**: computeScoreV2への切替(バージョンフラグでV1経路は
   リプレイ用に温存 — リプレイ互換は憲法)
4. 進めながら §9 のチェックボックスを更新すること
- **注意**: これはメインネットリセット前提の作業(後方互換の移行は最小・テストネットで新シーズン)。
  V1リプレイ(過去レース検証)だけは壊さないこと

## 4. オーナー待ち(2件のみ)

1. **A層の実機確認**(総合値・安全圏・未回収→確定→回収・広告費タブ)
2. **弁護士確認の結果**(=ジャックポット**本番公開**の解禁。実装は待たない)

## 5. 恒久指示・運用ルール(このセッションで確定)

- **弁護士対応は人間同士・Claudeは文章起草不要・弁護士を理由に開発を止めない**(公開ゲートのみ)
- **デプロイ禁止帯(20:00 MYT±1h)はデバッグ期間中(実ユーザー3名)停止** — テストネット
  試運転シーズン開始で再開
- フェーズ完了ごとにタグ(基点は `pre-fun-overhaul`)
- 未コミットのオーナー保留物は不変: `LEGAL_REVIEW_MEMO.md`修正・`EASTER_EGG_PLAN.md`・
  `operator-rtp-sim.mjs`・`法務.txt`・portrait画像3枚

## 6. このセッションのハマりどころ(新規)

- training_sessions は**UPDATE禁止トリガー**(typeの変更は delete+insert が正規の道 —
  スキーマコメントに「気が変わったらdelete」と最初から書いてある)
- `xmax = 0` によるupsert検出はトリガー環境では不要リスク — 事前SELECTで判定した
- PGliteテストはテスト順序で残高が汚れる — プラットフォーム勘定のアサーションは**差分**で書く
- Windows環境: 長いheredocは稀に引用崩れ → pythonスクリプトをファイルに書いて実行が確実
- ローカルdevサーバーは本番DBのプーラー枠(15)を食う — QA後は必ずkill
